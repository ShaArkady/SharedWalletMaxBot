Простой и удобный чат-бот для мессенджера "Max", который помогает вести учет совместных расходов. Идеально подходит для друзей, пар, соседей по квартире или коллег, чтобы отслеживать общие траты и легко рассчитываться друг с другом.

🚀 Основные возможности
- Создание общих счетов: Создавайте отдельные "кошельки" для разных целей (например, "Поездка в отпуск", "Аренда", "Подарок коллеге").

- Присоединение по ID: Участники могут легко присоединиться к счету по его уникальному номеру.

- Учет операций: Раздельное добавление личных пополнений (взносов) и общих трат.

- Просмотр истории: Каждый пользователь может посмотреть список своих операций в рамках счета.

- Получение быстрой статистики прямо в чате.

- Выгрузка подробного отчета со всеми операциями в формате PDF с итоговым балансом (кто кому сколько должен).

⚙️ Технологический стек
Язык: Python 3.13

- Фреймворк для бота: maxapi

- База данных: PostgreSQL

- Работа с БД: SQLAlchemy 2.0 (ORM) + asyncpg (асинхронный драйвер)

- Валидация данных: Pydantic

- Генерация PDF: ReportLab

- Контейнеризация: Docker, Docker Compose

⚡️ Быстрый старт: запуск проекта локально

Для запуска проекта на вашем компьютере потребуется установленный Git и Docker с Docker Compose.

1. Клонируйте репозиторий. Откройте терминал или командную строку и выполните команду:
```bash
git clone https://github.com/ShaArkady/SharedWalletMaxBot.git
```
2. Перейдите в директорию проекта
```bash
cd SharedWalletMaxBot
```
3. Настройте переменные окружения. Создайте файл .env в корневой директории проекта. Теперь откройте созданный файл и впишите туда необходимые данные. В первую очередь — токен вашего бота. Можно использовать .env из репозитория.

Пример содержимого .env:
```text
# Токен вашего бота, полученный в мессенджере Max
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

DB_USER=postgres
DB_PASSWORD=SECRET_PASSWORD
DB_HOST=db
DB_PORT=5432
DB_NAME=wallet_bot
```
4. Соберите и запустите Docker-контейнеры
Выполните одну команду, которая автоматически соберет образ вашего приложения и запустит два контейнера: один с ботом, другой с базой данных PostgreSQL.
```bash
docker-compose up --build -d
```
--build: эта опция заставит Docker пересобрать образ, если вы внесли изменения в код или Dockerfile.

-d (detached): запускает контейнеры в фоновом режиме.

Готово! После выполнения команды ваш бот запущен и готов к работе. Вы можете найти его в мессенджере "Max" и начать отправлять команды.

Остановка проекта

Чтобы остановить запущенные контейнеры, выполните команду:
```bash
docker-compose down
```
